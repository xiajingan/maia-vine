# Observability 模板

PromQL / LogQL 查询示例；指标名、窗口、SLO 和依赖必须按项目 Architecture 改写后再使用。
脚本 `harness observability-check` 只校验 `config/harness.yml#observability.required_assets` 声明的项目适用资产。

详见 `docs/CICD.md` 与 `docs/OBSERVABILITY.md`。
